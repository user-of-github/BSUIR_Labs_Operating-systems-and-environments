name: Run tests
on: [push]

jobs:
	run:
		runs-on: ubuntu-latest
		steps:
			- name: checkout repo
			  uses: actions/checkout@v2
			- name: build application
        	  run: |
        	  cd LR4
        	  cd replace_words
        	  make test
        	  make clean
